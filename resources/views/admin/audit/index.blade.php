<x-app-layout>
    <x-slot:title>Moderation audit</x-slot:title>
    <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8"><header class="mb-6"><p class="font-mono text-xs font-semibold uppercase tracking-widest text-red-700">Admin</p><h1 class="mt-1 text-3xl font-bold">Moderation audit log</h1></header><div class="overflow-hidden rounded-lg border border-slate-200 bg-white"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Action</th><th class="px-5 py-3">Moderator</th><th class="px-5 py-3">Subject</th><th class="px-5 py-3">Time</th></tr></thead><tbody class="divide-y divide-slate-200">@forelse($logs as $log)<tr><td class="px-5 py-4 font-mono">{{ $log->action }}</td><td class="px-5 py-4">{{ $log->user?->name ?? 'System' }}</td><td class="px-5 py-4 text-slate-500">{{ class_basename($log->subject_type) }} #{{ $log->subject_id }}</td><td class="px-5 py-4 text-slate-500">{{ $log->created_at->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-slate-500">No moderation actions recorded.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $logs->links() }}</div></div>
</x-app-layout>
